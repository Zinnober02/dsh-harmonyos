# dsh-harmonyos HarmonyOS profile overlay (web)
# 原则: 只动「原生依赖/设备上无意义」的行; 纯 JS 功能一律保留。
# sharp 在鸿蒙走 @img/sharp-wasm32(纯 wasm), 附件/读图可用 → 不禁用。
# subprocess: 真 node-pty(ensure-pty 编译) + 真 koffi(ensure-koffi 编译 + .codesign 签名,
# 沙箱 dlopen 必需; libc getpid/fcntl 已实测) → 终端/子进程启用。
- id: subprocess
  disabled: false
- id: sandbox
  disabled: false       # koffi(真原生, 预编译)已通 → sandbox 服务可用
- id: bash-sandbox
  disabled: false       # bash 存在(hnp GNU bash 5.3) + subprocess/sandbox 已通 → shell 服务可用
- id: open-in-app
  disabled: false       # 0.1.3 新增, 依赖 subprocess 服务; 随 subprocess 启用
# fs-search(glob/grep): 走 ctx.subprocess 强制沙箱后端且无权限升级参数; 鸿蒙无后端 → 禁用
- id: tool-fs-search
  disabled: false       # DSH_RG_PATH(预编译 musl rg) 已就位; danger 下可跑(实测 glob/grep 正常)
# auto 目录选择器解析 bind host/SSH(依赖被禁用的原生能力) → 换纯 JS browse 变体
- id: directory-picker
  name: '@deepseek-ai/dsh-host-directory-picker-browse'
